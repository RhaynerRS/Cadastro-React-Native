import { createNativeStackNavigator } from "@react-navigation/native-stack";

const AuthStack = createStackNavigator();

export default dunction AuthRoutes() {
    return (
        <AppStack></AppStack>
    )
}